pipeline {
    agent any
    environment {
        jenkins_demo = credentials('demo-secret')
    }
    stages {
        stage('demo') {
            steps {
                echo 'hello world'
                echo "my secret cred is: ${jenkins_demo}"
            }
        }
    }
}
